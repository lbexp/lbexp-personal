import { useEffect } from 'react';
import styles from './styles.module.css';

export default function Introduction() {
	return (
		<section>
			<h2 className={styles.title}>I'm Fiqar 'lbexp' Aprialim,</h2>
			<p className={styles.desc}>
				Currently pursuing my career as software engineer in a local software agency.
			</p>
		</section>
	);
}
